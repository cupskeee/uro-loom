# Optional: the server URL the Connection screen pre-fills. Copy to `.env` to override.
# The bearer TOKEN is entered at runtime and stored in sessionStorage — never commit it.
VITE_DEFAULT_SERVER_URL=http://127.0.0.1:8000
